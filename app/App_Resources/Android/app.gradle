// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "wise.talk.app"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
